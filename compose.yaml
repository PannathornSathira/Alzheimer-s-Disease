services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: alzheimer_trial
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - alzheimer_trial_data:/var/lib/postgresql/data

volumes:
  alzheimer_trial_data:
